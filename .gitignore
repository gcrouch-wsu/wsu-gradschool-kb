.next/
node_modules/
out/
dist/
coverage/
playwright-report/
test-results/
.env
.env*.local
*.tsbuildinfo
vercel.json.local
.next-dev.log

# Internal planning / source content (not part of the deployable app)
.claude/
*.doc
*.doc.docx

.vercel
.env*

# Image files (screenshots, exports, scratch assets) — managed assets live in
# Vercel Blob / the DB, not the repo. Force-add (git add -f) if one is ever needed.
*.png
*.jpg
*.jpeg
*.gif
*.webp
*.bmp
*.ico
*.svg
*.avif
*.tif
*.tiff
